(def target-indices [nums target]
  (#sort nums)
  (ret (#fold (@indexed nums) []
         (fn [acc x]
           (if (= (@$2 x) target)
               (ret (+ acc [(@$1 x)]))
               (ret acc))))))