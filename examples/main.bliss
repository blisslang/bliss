(def target-indices [nums target]
  (def aux [acc i num]
    (if (= num target)
        (List#cons i acc)
        acc))

  (->> nums
      (List#sort)
      (List#with-idx)
      (List#fold aux [])
      (List#rev)))