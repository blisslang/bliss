(def target-indices [nums target]
  (let nums (List#sort nums))
  (List#rev (List#fold-left
    (fn [acc i num]
      (if (= num target)
          (List#cons i acc)
          acc))
    [] (List#with-idx nums))))